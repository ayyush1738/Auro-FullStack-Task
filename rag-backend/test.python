import psycopg2

try:
    conn = psycopg2.connect("dbname=rag_db user=postgres password=1738 host=localhost port=5432")
    print("✅ PostgreSQL connected via pgAdmin!")
    conn.close()
except Exception as e:
    print(f"❌ Connection error: {e}")
